# Dépendances de la pipeline statistique (functions/)
# Python >= 3.9

# --- Cœur scientifique ---
numpy>=1.21
pandas>=1.3
scipy>=1.7
statsmodels>=0.13
scikit-learn>=1.0
matplotlib>=3.4
seaborn>=0.11

# --- Entrées/sorties Excel ---
# openpyxl : lecture .xlsx (preprocessing.extract_excel_columns / read_excel)
# XlsxWriter : écriture des résumés multivariés (Multivariate_Analysis, ExcelWriter)
openpyxl>=3.0
XlsxWriter>=3.0

# --- Modèles hiérarchiques bayésiens ---
# Requis UNIQUEMENT par HLM_Analysis.py (dépendance lourde, embarque PyMC).
# Commentable si l'analyse bayésienne n'est pas utilisée.
bambi>=0.12

# --- SCED bayésien (functions/SCED_bayesian.py) ---
# Estimation de l'effet de condition par MCMC + Bayes factor (Savage-Dickey).
# Dépendance lourde (PyMC/PyTensor) ; les modules SCED non bayésiens fonctionnent sans.
pymc>=5.10
arviz>=0.17
